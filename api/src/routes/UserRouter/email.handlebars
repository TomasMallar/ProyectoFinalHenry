<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }}</title>
  </head>
  <body>
    <h1>{{ title }}</h1>
    <ul>
      {{#each users}}
        <li>{{ this.name }}</li>
      {{/each}}
    </ul>
  </body>
</html>